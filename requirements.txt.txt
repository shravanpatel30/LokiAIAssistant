# LLM and HTTP
requests>=2.31.0

# Date parsing for reminders
python-dateutil>=2.8.0
dateparser>=1.2.0

# Scheduler for reminders
APScheduler>=3.10.0

# Windows toast notifications
windows-toasts>=1.0.0

# Voice input (speech-to-text)
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
keyboard>=0.13.5

# Voice output (text-to-speech)
piper-tts>=1.3.0

# App discovery (Windows shortcut targets)
pywin32>=306

# System tray and GUI
pystray>=0.19.0
Pillow>=10.0.0
PySide6>=6.6.0
markdown>=3.5.0

# pdf text extraction and summarizing
pdfplumber>=0.11.0

# System info queries
psutil>=5.9.0

# Calculator (safe expression evaluation)
simpleeval>=0.9.13